name: Enable Pages
on:
  workflow_dispatch:
permissions:
  pages: write
  id-token: write
jobs:
  enable:
    runs-on: ubuntu-latest
    steps:
      - name: Enable GitHub Pages (source = Actions)
        uses: actions/configure-pages@v5
        with:
          enablement: true
